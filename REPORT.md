## Task 1A — Bare agent

### What is the agentic loop?
The agentic loop is the core cycle that an AI agent follows to accomplish tasks autonomously: Perceive → Reason/Plan → Act → Observe → Iterate until task is complete.

### What labs are available in our LMS?
The agent read local workspace files instead of querying the real backend — it has no LMS tools yet, so it cannot return real data.
## Task 1 completed
